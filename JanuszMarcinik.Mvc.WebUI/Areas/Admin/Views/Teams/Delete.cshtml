@using JanuszMarcinik.Mvc.WebUI.Areas.Admin.Models.Teams;
@model TeamViewModel

@{
    ViewBag.Title = "Potwierdź usunięcie drużyny";
}

<h2 class="center-block">
    <span class="label label-danger center-block">@ViewBag.Title</span>
</h2>

@using (var form = Html.Bootstrap().Begin(new Form()))
{
    using (var panel = Html.Bootstrap().Begin(new Panel()))
    {
        using (panel.BeginBody())
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(x => x.TeamId)

            @form.FormGroup().TextBoxFor(x => x.DisplayedName).Readonly(true)
            @form.FormGroup().TextBoxFor(x => x.Name).Readonly(true)
            @form.FormGroup().TextBoxFor(x => x.City).Readonly(true)
            @form.FormGroup().TextBoxFor(x => x.Acronym).Readonly(true)

        }

        using (panel.BeginFooter())
        {
            <div class="btn-group">
                @Html.Bootstrap().SubmitDeleteButton()
                @Html.Bootstrap().BackButton(MVC.Admin.Teams.List(Model.LeagueId))
            </div>
        }
    }
}